--opt:speed
-d:release
-d:danger

# Keep compiler artifacts local and split by architecture to avoid
# mixed x86_64/arm64 object reuse when toolchains are switched.
@if amd64:
  nimcache=".nimcache/amd64"
@elif arm64:
  nimcache=".nimcache/arm64"
@else:
  nimcache=".nimcache/other"
@end
